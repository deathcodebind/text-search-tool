[package]
name = "search-engine"
version = "0.2.0"
edition = "2024"

[dependencies]
shared = { path = "../shared" }
storage = { path = "../storage" }
tantivy = "0.22"

[dev-dependencies]
tempfile = "3"
